# Statecharts for TS

This is a simple statechart library for TypeScript. It is inspired by the [XState](https://xstate.js.org/) library, but is much simpler and less powerful. It is intended for use in small projects where the full power of XState is not needed.

## Installation

```bash
npm install statecharts-ts
```

## Usage


